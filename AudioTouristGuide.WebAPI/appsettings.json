{
  "LetsEncrypt": {
    // Set this to automatically accept Let's Encrypt's terms of service.
    // If you don't set this in config, you will need to press "y" whenever the application starts
    "AcceptTermsOfService": true,

    // You must at least one domain name
    "DomainNames": [ "localhost" ],

    // You must specify an email address to register with letsencrypt.org
    "EmailAddress": "it-admin@example.com"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DevAzureSQL": "Server=tcp:***.database.windows.net,1433;Initial Catalog=***;Persist Security Info=False;User ID=admin;Password=***;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;",
    "DevLocalDb": "server=localhost;database=ATGDev;user=root;password=U3=gB+tk"
  },
  "AzureBlobStorageConfig": {
    "ConnectionString": "DefaultEndpointsProtocol=https;AccountName=***;AccountKey=***;EndpointSuffix=core.windows.net"
  }
}
